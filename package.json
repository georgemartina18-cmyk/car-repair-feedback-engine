{
  "name": "car-repair-feedback-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Reputation & Feedback Intelligence Engine for a multi-location car care / auto repair chain (n8n + Postgres + WhatsApp).",
  "license": "UNLICENSED",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "node scripts/build-workflows.js",
    "check": "node scripts/build-workflows.js --check",
    "test": "node --test tests/*.test.js",
    "test:db": "node scripts/test-db.js",
    "simulate": "node scripts/simulate.js"
  }
}
